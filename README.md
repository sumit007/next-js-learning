This is a starter template for [Learn Next.js](https://nextjs.org/learn).

## Requirements

- Node.js **20.9+** (recommended: **22** — see `.nvmrc`)

```bash
nvm use    # if you use nvm
npm install
npm run dev
```